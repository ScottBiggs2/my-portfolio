---
title: "Notes on DeepWeightFlow"
description: "Informal notes on motivation, surprises, and how weight-space generative models fit into the broader landscape."
pubDate: 2026-02-27
draft: true
---

This is a **sample post** written in Markdown. You can edit this file under `src/content/blog/` or add new `.md` files in the same folder.

## Why this setup

- Posts live as Markdown with frontmatter (`title`, `description`, `pubDate`, optional `draft`).
- The home page lists published posts; each post gets its own URL at `/blog/<filename-without-extension>`.
- Put images in `public/blog/` and reference them from Markdown, for example:

![Example image using your site banner](/banner.jpeg)

## Next steps

- Duplicate this file, rename it (the filename becomes the URL slug), and write your next note.
- Set `draft: true` to hide a post from the list and from `/blog/` routes until you are ready.
